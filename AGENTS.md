# 仙途站点开发规范

先读 README.md、docs/IMPLEMENTATION.md 与 docs/spec 中相关规则。用户最新要求优先，玩家主角由用户创建，固定 NPC 不能冒充主角。

保持 UI、纯规则、故事数据和图片资源的边界。所有世界写入经模拟 Worker，完整保存后才向 UI 确认；禁止重绘或图片回调推进时间、重抽随机、增加奖励。玩家与 NPC 共用修炼及突破规则，突破失败不致命。

修复涉及时间、资源、角色身份、记忆或存档的行为时运行相应规则测试及游戏类型检查。不要将计划验收、构建通过或 JSON 往返等同于真实浏览器／手机验收。

站点生命周期由负责此站点的主代理维护。子代理只处理明确分配的资产或研究任务，不改站点源码、身份、源仓库或部署。
