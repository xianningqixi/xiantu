# V0.1 开发与验收进度

本文件跟踪 2026-09-07 起在本地继续开发的实际进度。原 `docs/spec/` 是规格基线；任务完成须有实现和验收证据，不能把历史 `planned` 直接改为全部通过。

## 范围

完成 M0—M10 与 CP-01—CP-03 中当前版本的实现和可执行验收。金丹以上、完整恋爱路线、宗门经营、云同步、实时人物生图、完整 Mod 编辑器、运行时任意脚本包仍延期。保留原站点身份、素材、已存在的用户存档和公开迁移历史。

## 工作顺序

| 阶段 | 内容 | 状态 |
| --- | --- | --- |
| 1 | 存档协议、草稿、迁移、备份恢复、命令校验与重复保护 | done（追加故障回归通过） |
| 2 | 人物知情与记忆、承诺异常状态、NPC 冲突、五 Seed 回归 | done：最终 schema 4 五 Seed 全部通过 |
| 3 | 内容包协议、通用故事能力、作者预览、独立第二支线包 | 实现及规则、预览隔离验证通过 |
| 4 | 可关闭的 AI 交涉、服务端配置、提案校验与生命周期 | 规则与浏览器 mock 验证通过；真实供应方 not_run |
| 5 | PWA 安装、离线重开、版本安全更新 | done：生产离线、更新失败与安全点回归通过 |
| 6 | 浏览器主线与故障回归、设备矩阵、任务逐项证据与交付 | 本地 done；外部条件如实 blocked |

每一阶段完成相关验证后继续下一阶段；涉及保存结构的变更必须提供旧格式迁移，并在真实浏览器中验证原存档保留。

## 起点证据

- 分支：`codex/complete-v01`；源提交：`49ca87f10cbee1114d60f38461e77c64e8e815e6`。
- macOS，Node.js 26.3.0，npm 11.16.0：`npm ci` 与 `npm run verify` 成功；7 内容、8 Worker 存储、5 回调、21 规则测试全部通过，五阶段生产构建成功。
- Chrome / Playwright：开发页与生产页均可创建角色；做杂务到第 2 日，刷新后恢复；1440×1000 与 390×844 视口无横向溢出；页面无控制台错误；模板下载成功。
- 开发地址：`http://127.0.0.1:3000/`。以上不替代完整主线、跨标签、存储故障、离线或真机验收。

## 外部验收条件

真实 iPhone Safari、Windows Chromium、外部 3—5 名试玩者、真实 AI 供应方调用需要对应环境或参与者。没有实际执行前保持 `not_run` / `blocked`，本地浏览器模拟、mock 模型或代理不能充当这些证据。试玩提纲及供应方烟测入口由开发交付，邀请或联系他人需用户安排。

## 持续工作记录

- 已创建独立开发分支和本进度表；已设置完成／需用户处理事项的后续通知。

### 第一轮存档实现

- 新增 schemaVersion 2、严格命令／消息校验、稳定载荷回执；旧协议中无法核对载荷的历史命令保持拒绝重放。
- IndexedDB 升至结构版本 2，保留原 `xiantu-qingshi` 数据库并新增 drafts；结构升级不删除 saves/backups。旧快照在副本上迁移，完整验证并备份后原子切换。
- 草稿自动保存与恢复、过期草稿拒绝、本机备份列表／导出／确认恢复、损坏当前存档的原始数据导出已接入页面。
- 已通过：游戏类型检查，14 项生产 Worker 存储集成测试，5 项回调测试，7 项内容测试，8 项 Chrome 真浏览器测试（`PLAYWRIGHT_CHANNEL=chrome XIANTU_TEST_URL=http://127.0.0.1:3000 npm run test:browser:existing`）。浏览器测试使用隔离上下文。
- 浏览器 8 项覆盖草稿、双击、坏档与取消、跨页同 revision 换角色、备份恢复、实际文件往返、旧版迁移、坏档保留及恢复。配额／崩溃、离线与其他浏览器仍需后续验证。
- 补充通过：21 项规则测试（含 100 NPC / 3650 日）、2 项依赖边界／日志脱敏测试。规则核禁止直接访问 UI、网络、存储、现实时间和随机 API。
- 手机视觉核验发现创角页新增备份入口挤压导入按钮，已改为可换行布局并目视复核 390×844；备份弹窗标题、说明及关闭按钮可见。
- 浏览器截图工具在本机 GPU 路径出现画面未更新，切换 Chrome `--disable-gpu` 后，以动画结束、实际 DOM 尺寸和新截图共同验证；浏览器脚本已固定该启动参数。


### 后续实现与中间验证

- 当前保存结构为 schema 4／rules 0.1.2；从已发布无 schema／1／2 和本轮中间 schema 3 升级，保留原记录和精确官方内容锁。新知情记录包含当事人、目击、告知来源；普通交往按人物对保留累计摘要，不删除承诺、死亡或突破事实。
- 长行动记录父 ID、检查点与已支付灵石；客观无法履约有无违约惩罚的例外清算；致命遭遇、复杂模式死亡、简单模式救援分别结算，默认教学战斗仍非致命。日调度先处理寿元死亡，死者不能抢到当日动作。
- 阶段 2 五 Seed × 100 NPC × 3650 日全部通过，累计约 813.89 秒；40 日导出后续跑至 100 日与连续运行指纹相同。报告 `docs/reports/phase2-stress.json` 明确是 schema 3 中间版本，最终候选另测。完整格式化导出约 11.1—11.4 MB，统一导入限额调整为 16 MiB；不删历史来压缩测试。
- API 2 扩展协议提供严格 Schema、命名空间、能力与精确依赖校验；构建登记 JSON，新增剧情无需代码分支。新局选定 `contentLocks`，旧局不自动启用支线。原官方 API 1 兼容适配继续使用原 ID 与内容锁。
- CP-03 独立内容代理仅新增 `content-packs/guest-roadside/` 六个数据／文档文件，未改引擎或 UI。7 节点的归途口信支线经 5 项正式规则测试验证，包括贫穷、缺席、死亡、重复和坏版本；两个视觉槽位明确使用 planned 占位。
- `/author` 使用相同游戏规则与渲染器，Worker 写 `xiantu-author-preview`；预览存档不能导入正式游戏。真浏览器验证正式存档在创建预览、导入夹具及拒绝错误导入后不变。
- AI 提案走本站受控服务端路由，密钥仅服务端读取；6 项测试覆盖格式、限流、超时、配置、越权和共享知情。浏览器 mock 验证含糊条款不写入、明确确认只采用一次、HTML按文字显示、取消迟到无效、刷新历史不再次调用。真实供应方保持 not_run，见 `docs/AI-CONFIG.md`。
- 8 项原浏览器存档回归继续通过。新故障测试在真实 IndexedDB 上注入事务中止／同步配额异常、提交前终止 Worker、提交后抑制 ack，验证重新读取与立即重试不损档、不重复收益；另验证 versionchange 关闭旧连接。配额测试是故障注入，不伪称填满了实际磁盘。
- 320 CSS px ＋ 200% 字号＋阻断 `/art/` 图片时仍可创建与行动，无横向溢出；开发资源模块不属于图片故障注入范围。
- PWA 生产构建原子预缓存 HTML、脚本、Worker、内容、图片及图标；离线关闭页面后重开可继续劳务并刷新保存。生成的 public 路径在构建前登记，避免 vinext 静态路由漏收 postbuild 文件。更新测试通过实际 HTTP 代理切换服务脚本，模拟新部署；不靠运行时改文件绕过 vinext 的启动缓存。
- 完整主线浏览器验证已覆盖履约、30 日闭关暂停、重开、40 人档案、重逢与真实筑基严重失败；正在补完合法恢复后再次突破与另一分支，以及 Firefox/生产矩阵。

### 本地候选版收尾

- `npm run verify` 71项自动测试、strict与生产构建通过；五 Seed最终 schema4 共855.28秒、20,860条命令全通过，见 `reports/final-stress.json`。
- Chrome完整26项浏览器回归通过，Firefox原23项和补充检查点恢复通过；最终适配器／演示追加回归见 `reports/browser-final.json`。正常UI两条分支最终均至筑基；履约分支有真实失败及恢复。
- 修复本机Firefox部分完整URL请求导致生产刷新404，保留Vinext渲染并规范化同源请求目标。长行动请求ID由持久化父行动及检查点决定，重开仍保持稳定。
- 7份剧情节点真实Chrome下载／Firefox导入往返成功；桌面、390px预览和AI mock确认页已截图目视。最终交付材料和69条AC在 `reports/V01-LOCAL-ACCEPTANCE.md`，不覆盖历史记录。
- 原34项任务30项本地done，4项因独立评审／设备／试玩blocked；CP-01—03本地done。真实AI服务not_run，当前未部署、未推送或向他人发送邀请。
