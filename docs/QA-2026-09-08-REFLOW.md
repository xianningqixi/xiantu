# 整页重排验收 · 2026-09-08

最终本地服务：`http://127.0.0.1:3100`。构建版本：`02382c86f8b5c2c868f4`。

## 实际改动

桌面改为单行工具栏、紧凑人物状态栏、剧情主区与人物侧栏。地点返回、地名、天气和地图入口合为一行，场景图放在文字旁，底部行动改成紧凑按钮；减少重复标题、说明和大块留白。手机在剧情和此处人物之间切换，横屏低窗口恢复单行导航与侧边状态栏。人物资料各标签和重绘保持固定弹窗，目标导航可以从人物视图返回实际目标。

作者预览的提示条计入窗口高度，创角与游戏使用剩余空间，避免底部行动被提示条挤出窗口。没有更改模拟规则、人物身份、剧情数据或美术资源。

## 最终构建浏览器验证

使用本机 Chrome 的独立自动化上下文，未读取或覆盖用户正在使用的游戏存档，未调用外部生图服务。以下为浏览器视口验收，不是真实手机硬件验收。

| 窗口尺寸 | 顶部高度 | 剧情区域高度 | 行动条高度 | 结果 |
| --- | --- | --- | --- | --- |
| 1470×797 | 56px | 586.5px | 67.5px | 通过 |
| 1366×768 | 56px | 542.1px | 82.9px | 通过 |
| 1280×720 | 56px | 494.1px | 82.9px | 通过 |
| 390×844 | 93px | 495.1px | 74.4px | 通过 |
| 320×720 | 93px | 371.1px | 74.4px | 通过 |
| 320×568 | 93px | 219.1px | 74.4px | 通过 |
| 896×414 | 56px | 210.6px | 74.4px | 通过 |

七种尺寸均验证了文档尺寸与窗口相同、整页滚动位置为零；剧情推进后顶部与行动条位置稳定；打开资料、切换属性／关系／经历／立绘及重绘不改变弹窗高度；纯查看前后 IndexedDB 世界完全相同；杂务只推进实际的一日；五个主标签及返回地点选择均可使用。小屏的长正文允许在阅读区内滚动，手机底部行动可横向滑动。无页面脚本或控制台错误。

最终构建的四项现有浏览器回归全部通过（47.2 秒）：地点行动／宗门与真实费用；四卷素材经春水剧情、主线调查和地图前往夏夜并刷新保存；独立作者支线的原有显示；全部 120 张已登记 WebP 的实际 HTTP 响应。作者预览还验证了桌面和 390／320px 文档高度不超出窗口。

旧回归断言同步到当前产品：地名在地点标题，人物列表使用独立头像，大地图按需呼出，休息通过稳定的行动 ID 定位以兼容受伤后“调养伤势”的文案。未削弱实际时间、资源、图片身份与存档断言。

## 构建与资源

游戏类型检查、所改源码与测试的 Prettier 检查、生产构建通过。服务端与磁盘离线清单一致，18 个核心 URL 全部返回 200；离线核心 2,096,245 字节，低于既有 2 MiB 上限。验收完成后关闭本任务临时 3200 开发服务，保留 3100 游戏服务。

截图、七组几何数据、资源清单、自动化脚本与回归日志位于 `outputs/ui-reflow/`。桌面最终效果见 `outputs/ui-reflow/1470.png`，手机见 `390.png`，横屏见 `short-896.png`，实际夏夜剧情见 `summer-desktop.png` 与 `summer-mobile.png`。
