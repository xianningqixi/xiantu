# 2026-09-08 · 支线自有图片接入验收

已完成 API 2 的 owned 资源通道、《青石十钗》120 张图片和 11 位女角色立绘接入。当前工作仅在本地，未 commit、push 或部署。项目：`/Users/a1-6/Documents/ChatGPT/xiuxian`。

## 交付内容

- 契约增加可选 `entryFiles.art`、`art.assets`、`owned` 状态及人物定义的 `portraitId`。自有资源 ID 与 URL 都受本包命名空间限制；owned 只能引用本包实际声明的资源，角色立绘不能引用 scene。
- 构建校验缺图、PNG 签名／IHDR／尺寸和全局重复 URL；原 PNG 的 SHA-256 写入各卷 `integrity.json` 和登记表。无 art 包也采用 `sha256(JSON.stringify({content: data, images: {}}))`，因此 roadside 的锁同样变化。
- 一次汇总生成全部 WebP，避免分批清理误删已登记派生图。120 张新增图全部 `lazy: true`，最长边 1152，横图 1152×648，立绘 768×1152；原有 49 张派生图元数据及文件字节保持一致。
- 故事卡片并排展示 CG 与首位有立绘的参与者，立绘完整 contain。没有人物立绘的卡片保留旧布局。NPC 列表与详情采用自定义缓存图 → 支线自有立绘 → 既有图库／占位的显示顺序；若自定义缓存缺失，支线自有图可继续显示。没有修改缓存读取／生成／采用流程；内容定义的 portraitId 不写入 Actor 的图片哈希字段。
- 实际手机验收发现按钮提示中的未展开人物名称与长字符串会撑宽卡片，已在 UI 层展开人物名称并允许文字换行；没有改故事文件或推进规则。

| 包 | 新增 WebP 数 | 字节 |
| --- | ---: | ---: |
| shichai.chunshui | 31 | 3,078,392 |
| shichai.dongxue | 31 | 3,809,288 |
| shichai.qiudeng | 27 | 2,573,076 |
| shichai.xiaye | 31 | 2,840,936 |
| 合计 | 120 | 12,301,692 |

展示图片从 49 增至 169，总计 15,383,120 字节。每张派生图的 URL、尺寸、字节数与 lazy 状态保存在 [资源证据](../outputs/shichai-owned-integrity.json)。源 PNG 数量仍为 31／31／27／31，11 张 1024×1536 立绘与 109 张 1672×941 横图，未重画或改写。

## 命令检查（已执行）

环境：macOS、本机 Node v26.3.0、npm 11.16.0、Playwright 1.62.1 与 Google Chrome。Browser plugin not available，因此按前端测试技能使用仓库原有 Playwright；自带 Chromium 未安装，改用系统 Chrome，没有把无法启动浏览器算作验收通过。

| 命令 | 结果 | 证据／范围 |
| --- | --- | --- |
| `npm run content:prepare` | PASS | 5 个支线包登记，169 张展示图；包含在 extensions／verify 日志 |
| `npm run verify` | PASS，退出码 0 | 格式、边界、strict 类型检查、11 组共 105 项测试、本地构建及 UI 全套通过；[日志](../outputs/shichai-owned-verify.log) |
| `npm run test:extensions` | PASS，8 项 | 含 120 个 owned 槽位、11 张立绘、男性／roadside／PLAYER 空查找、非法引用／URL／scene 立绘与临时目录构建失败测试；[日志](../outputs/shichai-owned-extensions.log) |
| 独立 Shichai 测试命令（见下） | PASS，3 项 | 11 条道侣路线、苏清晏退让结局、顾听澜身故收束及全槽位 URL；[日志](../outputs/shichai-owned-shichai.log) |
| `npm run content:fixtures` | PASS | 5 包按当前动态锁生成隔离夹具，未依赖旧 lock；[日志](../outputs/shichai-owned-fixtures.log) |
| `npm run build:local` | PASS | 单独执行，客户端／Worker／服务端产物完整；[日志](../outputs/shichai-owned-build.log) |
| `PLAYWRIGHT_CHANNEL=chrome XIANTU_TEST_URL=http://127.0.0.1:3200 npx playwright test tests/browser/owned-art.spec.ts` | PASS，3 项 | 实际浏览器新局、手机宽度和全部图片 HTTP 检查；[日志](../outputs/shichai-owned-browser.log) |

```bash
npx esbuild tests/game/shichai.test.ts --bundle --platform=node --format=esm --outfile=.game-test-build/shichai.test.mjs --log-level=error && node --test .game-test-build/shichai.test.mjs
npm run dev:local -- --hostname 127.0.0.1 --port 3200
```

首次沙箱执行本地监听出现 EPERM；后续在允许本地监听的环境执行完整 verify，退出码 0。构建仍有 Vite 的大 chunk 提醒及 Node `module.register()` 弃用提示，未隐藏为零告警。测试代码明确等待现有的 400ms 重复点击保护，避免把快速自动点击抑制误判为游戏失败。

## 真实浏览器验收

两个独立浏览器上下文都从 `/author` 创建新局，没有导入夹具、直接写 IndexedDB 或伪造世界数据。

1. 勾选四卷，以默认 seed 12345 创建“图片验收”。真实首日坊市出现苏清晏相识节点；CG 的实际 src 命中 `suqingyan-cg-meet-*.webp`，旁边立绘命中 `suqingyan-portrait-*.webp`，两者 `complete` 且 `naturalWidth > 0`。点击相识选项后已保存 met 标记，revision 仅增加 1。
2. 进入“故人”，苏清晏列表头像和详情全身图均命中同一 owned 资源，详情图真实加载。观看、改变视口、重新加载页面及查看人物前后，完整 IndexedDB 存档相等；未改变时间、随机流或资源。
3. 四卷新局在 1440×1000、390×844、320×844 查看卡片；两种手机宽度 `scrollWidth <= innerWidth` 且卡片边界在屏幕内。立绘从头到鞋完整显示。
4. 另一局只选 roadside，经旅行与一日歇息等候真实 NPC、记下口信、坊市留话、返回古道，实际到达 `guest.roadside.return-with-words`。待绘占位仍显示，无立绘区块、无虚假 img；桌面及 390px 手机均可操作，选择后 closed 标记保存。
5. 全部 120 个优化资源请求均返回 HTTP 200、image/webp，响应长度与登记字节数一致。两个页面流程没有 console error／pageerror，未出现 Vite 错误覆盖层。

截图均已打开目检：

- [四卷新局桌面](../outputs/shichai-owned-desktop.png)、[完整相识卡片](../outputs/shichai-owned-card.png)
- [390px 手机](../outputs/shichai-owned-mobile-390.png)、[320px 手机](../outputs/shichai-owned-mobile-320.png)
- [NPC 全身图详情](../outputs/shichai-owned-npc.png)
- [roadside 桌面占位](../outputs/roadside-planned-desktop.png)、[roadside 手机占位](../outputs/roadside-planned-mobile.png)

浏览器验收只实际游玩上述路线，并未逐个游玩其余十位人物的所有分支。11 条完整道侣路线的证据来自规则测试，120 张全覆盖的证据来自契约、PNG／WebP 解码、文件摘要及 HTTP 检查；不能等同于 120 个画面全部经过游戏内逐张目检。此处“手机”指 Chrome 窄视口，未声称真实 iPhone／Safari 验收。

## 离线核心与兼容性

`dist/client/offline-manifest.json.bytes`：改前 1,420,407 → 验收快照 2,231,332（增加 810,925 字节）；验收快照清单 21 项。共享工作区的其他任务可能继续构建，数字以资源证据文件的本次快照为准。原始基线构建与当前构建的代码／登记内容也发生变化，因此这里如实报告核心增加，而不声称字节数完全不变。明确验证 120 张新增 WebP 没有任何一张进入核心，未增加 12.30 MB 的图片预缓存。按需图离线且从未访问时可显示占位，本轮未重复执行整站 PWA 断网安装测试。

官方 lock 改前／改后均为：

```text
official.qingshi@0.1.1:c7402c381fe903eaa99784deae2066993da76500f5ea9422e61ae40779670cd9
```

官方目录每个文件、四卷 storylets.json／outline.md／art-prompts.md 与全部原 PNG 的 SHA-256 均与开工基线相同。官方源 integrity 文件内容不变，准备脚本对相同内容不重复写入。原 49 张派生图也与基线一致。旧官方存档不重锁、未知支线锁拒绝读取并保留原档的回归测试通过；没有增加热升级或旧锁别名。旧四卷或 roadside 存档须使用原构建恢复。

## 共享工作区边界

开工时已有四卷数据、原 PNG、`shichai.test.ts` 和登记表修改，本轮在其基础上接入。执行途中另有并行的“立绘特征”修改进入 `creation.tsx`、`portrait-studio.tsx`、`panels.tsx`、`app/globals.css`、`lib/game/portrait-features.ts`、`portrait-library.ts`、`portrait-subject.ts`、`types.ts`、`protocol.ts`、`validate.ts`、服务端提示词与相关立绘测试。它们没有被覆盖或回退；同文件修改已保留，只做必要的格式检查／格式化。

这些并行的规则／协议／生成特征变更不属于本图片接入任务。资源证据文件中的 `sourceChanges` 如实记录相对开工基线发生变化的五个既有纯游戏文件，不把整个共享工作区宣称为“规则零差异”。本任务自身没有改引擎、Worker、存档 schema 或图片生成缓存。完整 verify 针对当前合并工作区执行，不代表其他任务的独立产品验收已完成。
