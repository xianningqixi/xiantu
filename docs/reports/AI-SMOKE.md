# 真实供应方烟测记录

状态：not_run（尚未提供服务端凭证和模型配置）。本文件不包含或收集密钥。

1. 在服务端安全配置 `docs/AI-CONFIG.md` 中的变量，保持 `XIANTU_AI_MOCK` 关闭。
2. 正常创角并与林晚相识，在她与周安均空闲在场时打开“自由交涉”。提交“凝元草给你，其他东西平分”：预期澄清且世界日期、资源、revision 不变。
3. 提交“你、我、周安下一次秘境同行，第一株凝元草归你，其余战利品归我，出发路费 2 灵石”：核对标准条款，确认后只有一份同行约定。刷新再查历程不得产生新模型请求。
4. 记录日期、操作系统、浏览器、配置的模型 ID（不含密钥）、请求数、各次等待毫秒、HTTP 状态、clarify / invite 等结构结果和最终约定。检查浏览器请求、响应和导出文件无凭证。

实际记录：待执行。模型自由文本不承诺重新调用时完全相同；已采用正文与规则输入由存档保存。
