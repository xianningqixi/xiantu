# 2026-09-09 仓库整理与上传检查

目标仓库：<https://github.com/xianningqixi/xiantu>，分支 `main`。整理前本地与 `origin/main` 一致，本轮收录此前积累的项目工作；没有重写远端历史。

## 本次内容

- 四城地图、八节点主线、四卷人物故事、三宗修行及NPC交友/远行/入宗行为。
- 人物属性、关系、经历、立绘编辑与恢复，以及两阶段LLM提示词/生图工作流。
- 120张四卷原图、独立人物立绘与头像及其目录；发布时派生WebP按需加载。原始图片保留以支持内容摘要校验，不用压缩改图改变既有内容锁。
- 74项UI评审的本轮处理、测试、文档与代表截图；68项已实施、6项部分完成，另有危急自动撤退策略缺口，详见 [UI交接](UI-REVIEW-IMPROVEMENTS.md)。

## 排除范围

`.env.production.local`、`.xiantu-private/`、浏览器存档/导出、node_modules、构建输出、测试缓存和本机工作目录不上传。补充了Playwright输出目录的忽略规则。没有复制项目外完整评审目录、源码基线压缩包或真实AI配置。

源文件凭证检查未发现真实密钥；两处含账号密码的URL均为测试中明确拒绝 `user:pass` 的假地址。图片单文件约3.2MB以内，无超过GitHub单文件100MiB上限的资产；原图保留在常规Git中，拉取时无需额外LFS工具。

## 上传前检查

本机 `v26.3.0` / npm `11.16.0` 执行 `npm run verify` 退出0，包括全项目格式、纯规则边界、游戏strict类型、内容及扩展、AI/模型、立绘、Worker存储、回调、规则、经济和UI组件，以及生产构建。

| 检查 | 结果 |
|---|---|
| `test:boundaries` | 3/3 通过，0 失败 |
| `test:content` | 7/7 通过，0 失败 |
| `test:extensions` | 31/31 通过，0 失败 |
| `test:ai` | 7/7 通过，0 失败 |
| `test:models` | 12/12 通过，0 失败 |
| `test:portraits` | 22/22 通过，0 失败 |
| `test:storage` | 27/27 通过，0 失败 |
| `test:client` | 5/5 通过，0 失败 |
| `test:game` | 71/71 通过，0 失败 |
| `test:economy` | 2/2 通过，0 失败 |
| `test:ui` | 2/2 通过，0 失败 |

详细机器摘要见 [verify-summary.json](ui-review-2026-09-08/verify-summary.json)。生产构建指纹 `4220bdf35b9d1c16c50c`，与当前3100实际返回的离线清单一致。整理前的UI实现逐文件散列仍与上轮验收记录一致；本次不把工程检查称为新的浏览器全面验收，也未调用真实AI供应商。

原始浏览器套件仍包含旧流程/文案断言。本次实际浏览器证据、不同构建的复测范围、真机及供应商未验项见UI交接；不会以本次上传宣称所有功能或所有终局已验收。

## 干净副本验证

从待提交的Git树用`git archive`导出独立副本，不带node_modules、构建缓存、个人配置和真实存档。重新执行`npm ci --no-audit --no-fund`及`npm run verify`均退出0，189项自动测试通过。内容准备后所有仓库文件逐字节不变，构建指纹与主工作区相同；证明原始图片、内容锁与依赖可从本次提交内容重建。

## 新环境运行

```sh
npm ci
npm run verify
npm run start:local -- --hostname 127.0.0.1 --port 3100
```

图片、内容锁、下载模板与离线资源由构建前后脚本准备。固定选项游玩不需要密钥；可选模型配置按 [AI-CONFIG](../AI-CONFIG.md) 在新环境自行设置。新域名/端口的浏览器存档独立，迁移前需导出，不能用清空原浏览器数据替代迁移。
