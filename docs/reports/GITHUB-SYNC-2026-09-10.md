# 2026-09-10 主分支整理与同步

本次按用户授权，在 UI 重构、成长与日循环、美术三个项目任务及协调任务全部交付后，整合到 `main`。仓库为 `xianningqixi/xiantu`；工作区为 `xiuxian/xiantu`，不是父目录的空仓库。以下是合并后的检查，原分支报告仅作为对应阶段的历史证据。

## 纳入内容

| 来源 | 交付点 | 整合结果 |
| --- | --- | --- |
| B：成长与日循环 | `7930153ab5b84431b9dd7eccc536f5c4fddfad0f`，已有合并点 `9d79fd2` | 13 档境界、逐日事件、差事与经济、功法／洞府及规则 0.2.0 迁移。 |
| A：道场 UI 与 A/B 适配 | `fdeed799b310c7223e967ded3a5083d44c7e58d9` | 道场／游历／人物／行囊四入口、事件流、突破准备和仪式、精简创角。 |
| 本地模型与布局修复 | `79ac17c` | Node 本地运行与模型诊断、创角底部状态不挤成竖排、立绘操作区与图片分开、去除重复设置入口、工具按钮等高。 |
| 20260910 美术交付 | `c09b57f` | 120 张新立绘、123 张新场景；14 个素材位保留旧图，详见素材说明。 |
| UI 设计资料 | `db8d473` | 保留 UI 重构方案 HTML 与参考截图。 |

创角和道场文件存在布局冲突，采用 A 的最终页面结构，保留本地修复的行为。「更多设定」中的立绘容器恢复自然高度，操作按钮在图片区下方；模型设置与本机备份共用尺寸。旧右侧人物栏由分组人物页替代，同行、已结识、此地在场互不重复，各组及完整列表使用同一行组件。

所有世界修改仍经 Worker 保存确认。图像、查看面板和动画没有新增时间推进或奖励逻辑；没有修改 B 的数值、迁移及纯规则来适配展示。

## 文件与素材

- 正式资源位于 `content-packs/art-refresh-20260910/`，包括 257 个 WebP、逐图 SHA／身份映射、提示词来源与审核记录；构建引用已切到本批。
- [257 个素材位新旧对照](ART-REFRESH-2026-09-10.html) 使用仓库内缩略图和 WebP 相对路径，克隆仓库后可直接打开。全部 771 个图片引用已核对存在。
- 129 张头像中 120 张随本批立绘更新，9 张沿用旧图；全部 19 个地点采用新图。14 个保留项及原因见 [素材说明](../../content-packs/art-refresh-20260910/README.md)，没有计为新生成。
- A/B 原报告和截图继续保留。本轮截图与量化记录使用独立的 `screenshots/final-sync/`，临时日志、浏览器痕迹和测试输出归入已忽略的 `outputs/diagnostics/final-sync/`。
- 合并前对 287 个本地改动／未跟踪文件做逐字节备份，共 81,058,752 bytes；位置为父目录 `integration-backups/final-sync-20260910-183201/`，包含文件、SHA 清单和 Git 补丁。其他工作树、旧备份、制作目录与原生 PNG 均保留。
- API 密钥、真实玩家存档、服务私有目录、依赖、构建文件和临时生产输出不纳入 Git。

## 合并后验证

实际环境：Node 23.9.0、npm 10.9.2；Chrome；生产服务使用 `npm run start:local -- --hostname 127.0.0.1 --port 3101`，与用户的本地开发服务分开。

`npm run verify` 全部通过，包含格式、依赖边界、严格游戏类型检查、生产构建和以下 224 项测试：

| 测试组 | 通过数 |
| --- | ---: |
| HTTP／模块边界 | 3 |
| 内容与扩展 | 8 + 34 |
| AI 与模型配置 | 7 + 23 |
| 立绘 | 22 |
| 存储与客户端 | 29 + 6 |
| 游戏规则 | 87 |
| 经济与 UI 契约 | 3 + 2 |

| 浏览器／最终检查 | 结果 |
| --- | --- |
| Chrome 全套，1 worker | 70 passed／1 failed，0 skipped，19.8 分钟。唯一失败为沿用上一批素材的 300 KiB 立绘体积上限。 |
| 最终构建专项：美术、UI 改进、模型设置、立绘、A/B 首屏与存档 | 19/19，0 skipped，2.5 分钟。包含原失败项。 |
| 最终格式检查与 `npm run build:local` | 通过；离线版本 `a62baa5a1303c116efbb`，18 个核心文件，2,214,087 bytes。 |
| Playwright CLI 手动编排布局验收 | 1440／390／320px 的生成等待、新旧对比、状态条及等高按钮全部通过。 |
| 人物页桌面／手机验收 | 概览 7 行、完整列表首页 24 行均无重复 ID；行高分别统一为 110／98px，无横向溢出；浏览前后完整 World 相同。 |
| 可携带美术对照页 | 257 项、243 张新图、14 项保留；新旧原尺寸图片可打开，390px 无横向溢出。 |

新版全分辨率立绘最大 379,110 bytes（约 370 KiB）。将旧测试预算从 300 调整为 400 KiB，保留逐图字节、SHA、尺寸、头像对应、按需加载及不进入初始离线核心的检查。没有压缩或换图来掩盖审核素材差异，也没有跳过失败项。71 个浏览器用例均有合并后的成功执行记录：首轮通过 70 个，原失败项及相关 18 项在最终构建上重跑通过；不是宣称首轮 71/71。

三个创角工具按钮在全部检查宽度下均为 44px 高、13px 字号，AI 设置仅出现一次。立绘对比框等高，操作区位于内容区下方；生成状态文字保持 19.5px 行高，未挤成竖排。最终构建自动化从填写姓名到首次修为满条分别为桌面 1,854ms、手机 1,736ms。

20 张本轮截图、原始量化记录和素材摘要见 [截图索引](screenshots/final-sync/README.md) 与 [机器记录](screenshots/final-sync/verification.json)。首轮失败诊断、完整通过日志和其余 67 张回归截图保留于本机 `outputs/diagnostics/final-sync/`；原 A/B 证据已从合并索引还原，没有被本轮重拍覆盖。

浏览器大存档检查使用已有 seed 1／3650 日／100 NPC 的合成测试夹具，984,974 bytes，SHA-256 `d1e43c7ea92f3a4ae98528b5fbd601c5c4e57475dbdb505036ad6b7d23d8b555`；不是本轮新执行十年压力模拟。实际导入 546ms，解锁后 30 日等待及查看耗时 9,787ms，50ms 采样的最大 UI 定时器间隔 144.5ms。此为回归环境测量，不等同于真人体验评测。

本轮模型与生成布局验收使用隔离浏览器及受控接口响应；未重复付费生成，也未使用或覆盖玩家正在使用的存档。逐图加载和摘要验证使用正式素材。

## 保留的开放项

高资质相对低资质耗时仍为 8／13 日，节省 38.46%；洞府仍只记录当前城市，换城购买会替换；后续技能、泛化同行等机制以已实现命令为限。14 个美术保留项与逐图取景限制保持原审核结论。本轮没有为消除这些记录而扩大规则或美术范围。

## Git 同步

最终提交为保留 A/B 历史的正常合并；目标 `origin/main`。使用普通快进推送，并在推送后比较远端与本地 SHA。可通过 `git log -1 --format=%H -- docs/reports/GITHUB-SYNC-2026-09-10.md` 定位整合提交，查看最终代码及两条父提交链；远端同步结果由本次任务交付消息记录。

本机 `http://127.0.0.1:3000` 已启动相同的最终生产构建，使用 `npm run start:local -- --hostname 127.0.0.1 --port 3000`。浏览器测试使用的 3101 端口仅为隔离验收服务。未发布站点或改动托管身份。
