# 执行规格包检查记录

检查日期：2026-09-06。对象：V0.1.1-Web 文档、契约及内容样例。

这份记录证明规格包进行了静态检查，不代表游戏已经实现或满足软件验收。

## 已执行

| 检查 | 方法 | 结果 |
| --- | --- | --- |
| 配置与故事一致性 | 在包根目录运行 `python3 tools/verify_pack.py` | PASS；1,247 项静态断言，无错误 |
| 内容范围 | 校验器读取实际 JSON 和任务／验收编号 | 14 个故事节点，8 项计划素材，34 个任务，69 项验收 |
| TypeScript 可解析性 | Node v24.19.0 执行 `node --experimental-strip-types contracts/game.ts` | 退出码 0；仅类型剥离与语法可解析性，不是 strict 类型检查 |
| 跨文档规则复核 | 对照 GDD、TDD、命令说明、配置和故事样例 | 修正了小层自动提升、一次性事件消费、秘境入口约束、NPC 筹备目标、非致命战斗恢复、功法来源、每日行动额度和同行期间调度规则 |

校验器的断言主要覆盖内容 ID、角色与地点引用、命令字段、条件白名单、数值范围、故事跳转、任务依赖顺序及验收编号。数量用于复查执行结果，不表示有 1,247 个游戏行为测试。

## 尚未执行

- TypeScript strict 类型检查、依赖安装、构建与软件单元测试。
- 100 人／3,650 日世界模拟和随机回放。
- IndexedDB 事务、浏览器刷新恢复、多标签页和存档迁移测试。
- 桌面与真实手机浏览器验收、离线缓存和性能测量。
- AI 真实供应方调用、正式图片制作及玩家试玩。

`TASKS.md` 的任务均为 `planned`；`ACCEPTANCE.md` 的软件验收均未执行。资产清单中的 `planned` 及空 `filePath` 是素材任务状态，不是失效下载链接。`contracts/game.ts` 是实施契约草案，不包含可玩的游戏实现。

后续应在目标工程为每个 Task 提交实现、测试命令和实际结果，并将失败、受阻与未运行分别记录。

## 内容协作补充检查

新增 CONTENT_PACKS.md，明确大纲、运行故事、资源与存档边界，并新增3项 planned 的 CP任务。README、TDD、TASKS、启动指令和主设计已加入入口或前置门槛。上述静态检查数量仍对应原型数据、34项原任务和69项原验收；不包含新增内容包协议的运行验收。新包清单、通用能力、作者预览和第二作者包均待实现。
